# Assets

- `logo.png` – square logo (use the high-quality PNG from the design session)
- `logo.svg` – vector version (placeholder geometric)
- `social-preview.png` – Open Graph / GitHub social image

Upload the two PNG files from the conversation artifacts into this folder after merging, or add them via the GitHub web UI.
